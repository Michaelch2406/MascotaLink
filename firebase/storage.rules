rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow reads and writes for all authenticated users.
    // For production, you should restrict this further.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}