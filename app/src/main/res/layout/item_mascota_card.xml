<?xml version="1.0" encoding="utf-8"?>
<!-- Item de mascota para MisMascotasActivity - Replica diseño HTML mismascotas.html -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_mascota"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:stateListAnimator="@animator/item_press_scale"
    app:cardBackgroundColor="@color/white"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:minHeight="100dp"
        android:orientation="horizontal"
        android:padding="12dp">

        <!-- Avatar circular con borde - 76dp como en HTML -->
        <androidx.cardview.widget.CardView
            android:layout_width="76dp"
            android:layout_height="76dp"
            app:cardCornerRadius="38dp"
            app:cardElevation="1dp"
            app:cardBackgroundColor="@color/white">

            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/iv_mascota_avatar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/foto_principal_mascota"
                app:civ_border_color="@color/white"
                app:civ_border_width="3dp" />

        </androidx.cardview.widget.CardView>

        <!-- Información de la mascota -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Nombre - text-[18px] font-bold -->
            <TextView
                android:id="@+id/tv_mascota_nombre"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/black"
                android:textSize="18sp"
                android:textStyle="bold"
                tools:text="Max" />

            <!-- Raza y sexo - text-[14px] text-gray-600 -->
            <TextView
                android:id="@+id/tv_mascota_raza_sexo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/gray_dark"
                android:textSize="14sp"
                tools:text="Golden Retriever - Macho" />

            <!-- Edad y peso - text-[12px] text-gray-400 -->
            <TextView
                android:id="@+id/tv_mascota_detalles"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="@color/gray_text"
                android:textSize="12sp"
                tools:text="3 años • 28 kg" />

        </LinearLayout>

        <!-- Chevron derecho -->
        <ImageView
            android:id="@+id/iv_chevron"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="8dp"
            android:padding="4dp"
            android:src="@drawable/ic_arrow_forward"
            app:tint="@color/gray_disabled"
            android:contentDescription="@null" />

    </LinearLayout>

</androidx.cardview.widget.CardView>
